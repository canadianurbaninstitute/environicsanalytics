% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mobilescapes-api.R
\name{get_mobilescapes_destination_summary}
\alias{get_mobilescapes_destination_summary}
\title{Get MobileScapes Destination Summary Report}
\usage{
get_mobilescapes_destination_summary(
  geofence_ids,
  start_date,
  end_date,
  days_of_week = NULL,
  time_of_day = "AllDay",
  dwell = "Any",
  country = "ca",
  vintage
)
}
\arguments{
\item{geofence_ids}{Character vector. EA geofence IDs. Only `"EA Standard"`
geofences (ID prefix `E`, e.g. `"E12345"`) work here - `"Custom"`
geofences (ID prefix `C`) are rejected with `400 Invalid geofence
ID(s)`, even though the Envision UI presents both types identically.
A short date range can also legitimately return zero rows for a valid
`E`-prefixed ID with no visits in that window - that is not the same
as an invalid ID. See docs/v4-to-v5-changes.md for both, verified.}

\item{start_date}{Character. Start date in "YYYY-MM-DD" format.}

\item{end_date}{Character. End date in "YYYY-MM-DD" format.}

\item{days_of_week}{Character vector. Optional. "Mon" through "Sun".}

\item{time_of_day}{Character. Optional. One of "AllDay", "Morning",
"Afternoon", "Evening". Default "AllDay".}

\item{dwell}{Character. Optional. One of "Any", "Short", "Medium",
"Long". Default "Any".}

\item{country}{Character. 2-digit country code. Default "ca".}

\item{vintage}{Character or numeric. Dataset vintage (e.g. "2026").}
}
\value{
List with keys `visitSummary`, `weekdayWeekendSummary`,
  `originSummary`, `topSegments`, `visitBreakdowns`, `demographics`.
  Returned as a list (not flattened) since the structure is nested.
}
\description{
Returns a detailed summary for one or more destination geofences,
including visit totals, weekday/weekend split, visitor origins,
top audience segments, visit breakdowns, and demographics.
}
